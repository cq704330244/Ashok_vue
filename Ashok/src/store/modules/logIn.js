const loginModules = {
  state: {
    userInfo: 'chengqi'
  },

  mutations: {
    storeUserInfo(state, playload) {
      state.userInfo = playload
    }
  },

  actions: {},

  getters: {}
}
export default loginModules
